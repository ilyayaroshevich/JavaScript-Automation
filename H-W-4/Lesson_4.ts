/*-----------------------------------------СТРОКИ, ЧИСЛА, ДАТА, РЕГУЛЯРНЫЕ ВЫРАЖЕНИЯ---------------------------------------
                                                            [СТРОКИ]
    str.CharAt() - Показывает индекс символа в строке;
    str.Concat('var1','var2') - Соединяет строки;
    str.endWith('word') - Проверяет, что строка заканчивается на определённом слове;
    str.match()
    str.normolize()
    str.replace()
    str.search()
    str.slice()
    str.startWith()
    str.substring()
    str.toLocaleLowerCase()
    str.toLocaleUpperCase()
    str.toString()
    str.trim()
    str.includes()
    Повторить тернарные операторы
    str.indexOf()
                                                            [ЧИСЛА]
    num.isInteger()
    num.parseFloat()
    num.parseInt()
    num.toFixed()
    Math.abs()
    Math.ceil()
    Math.floor()
    Lodash  -  библиотека
    Math.random()

                                                            [ДАТА]
    new Date()
    date.getFullYear()
    date.getFullMonth()
    date.getFullDate()
    date.getFullHours()
    date.getFullMinutes()
    date.getFullSeconds()
    date.getTimezoneOffset()
    date.getUTCDate()
    date.toISOString()
    date.toLocaleTimeString()
    date.UTC()
    date.toLocaleString()
    date.setDate()
    moment.js   -   библиотека




-----------------------------------------СТРОКИ, ЧИСЛА, ДАТА, РЕГУЛЯРНЫЕ ВЫРАЖЕНИЯ---------------------------------------*/
// [ПРИМЕР math.random!!!]

// console.log('Math.random()');
// {
//     //   Min and Max            5    10
//     function getRandomArbitrary(min, max) {
//         //0.465489 * (10 - 5) + 5
//         return Math.random() * (max - min) + min;
//     }
//     let doo1 = getRandomArbitrary(5, 10);
//     let doo2 = getRandomArbitrary(5, 10);
//     console.log(doo1.toFixed(2) + ' ' + doo2.toFixed(2));
// }
