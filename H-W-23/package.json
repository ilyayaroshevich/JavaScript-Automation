{
  "name": "h-w-23",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "playwright_test": "rmdir /s /q allure-report && rmdir /s /q allure-results && playwright test --config=./playwright.config.ts",
    "allure_report": "allure generate allure-results --clean -o allure-report && allure open allure-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.44.1",
    "@types/node": "^20.12.12",
    "allure-commandline": "^2.29.0",
    "allure-playwright": "^3.0.0-beta.3",
    "expect-playwright": "^0.8.0"
  }
}
